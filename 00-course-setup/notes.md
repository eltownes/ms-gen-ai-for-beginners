### create/mod environments from the `Anaconda Prompt`

### may need to restart vscode


`conda env create -f ms-gen-ai-0.yml`

`conda env list`

`conda activate ms-gen-ai-0`

`conda list`

`conda install PACKAGE_NAME`

`conda deactivate`

`conda list -n ENV_NAME`
